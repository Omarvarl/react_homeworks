// import TaskList from 'widgets/taskList/ui/TaskList';

// Из-за фильтрации TaskList вся логика перенесена в компонент TaskList
// Оставляю этот компонент по заданию, но в нем нет необходимости.
// Плюс ко всему здесь нарушается fsd импортом из в виджет (понимаю, что это допустимо, но мне кажется так лучше).
export function TaskWidget() {
    // return <TaskList />;
}
